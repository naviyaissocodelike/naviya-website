{
  "name": "naviya-website",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "^20.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp -r pages dist/pages && cp -r css dist/css && cp -r js dist/js",
    "start": "npx serve dist -p ${PORT:-3000}",
    "preview": "vite preview --port 5000"
  },
  "dependencies": {
    "framer-motion": "^10.12.16",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^7.16.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^5.0.0"
  }
}
